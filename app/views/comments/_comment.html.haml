.comment
	.well
		=gravatar_for(comment.user, 50)
		=link_to comment.user.username, user_path(comment.user), class:"comment-username"
		#{time_ago_in_words(comment.created_at)} ago

		%p.comment-content #{comment.content}
		-if user_signed_in?
			-if comment.user_id == current_user.id
				=link_to post_comment_path(@post, comment), method: :delete, data:{confirm:"Are you sure?"}, class:"comment-delete" do
					%i.fa.fa-close
		

